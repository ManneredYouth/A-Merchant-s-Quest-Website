import { Link } from "react-router-dom";
import Header from "../prefabs/Header.js"

export default function About() {
  return (
    <div>
      <nav>
        <Header />
      </nav>
      <h1>About!</h1>
    </div>
  );
}