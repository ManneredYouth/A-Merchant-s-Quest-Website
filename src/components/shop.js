import { Link } from "react-router-dom";
import Header from "../prefabs/Header.js"

export default function Shop() {
  return (
    <div>
      <nav>
        <Header />
      </nav>
      <h1>Shop!</h1>
    </div>
  );
}